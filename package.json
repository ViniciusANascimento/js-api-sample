{
  "name": "api-sample",
  "version": "1.0.0",
  "main": "./lib/index.js",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "lint": "tslint -c tslint.json src/**/*.ts",
    "prepublish": "npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "tslint": "^6.1.3",
    "typescript": "^5.6.2"
  },
  "files": [
    "./bin/*",
    "./lib/*"
  ],
  "devDependencies": {
    "tsup": "^8.3.0",
    "tsx": "^4.19.1"
  }
}
